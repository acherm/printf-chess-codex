#include <stdio.h>

/*
 * Generated by tools/c2pop.py from: /tmp/micro_run_zero.c
 *
 * Source loop statements:
 *   d[RUN] = 0;
 *   pop_print("A\n");
 *
 * POP Compliance Report
 * Canonical loop: while (*d) printf(fmt, ARGS);
 * Purity score: 8/10 (hybrid POP)
 * - Loop purity: 2/2
 * - Mutation purity: 2/2
 * - Control/branch purity: 0/2
 * - Engine purity: 2/2
 * - No C-script ROM: 2/2
 * Tape bytes:
 * d[0] (RUN)
 * d[1] (IDX_1)
 * d[2] (IDX_2)
 * d[3] (IDX_3)
 * d[4] (CONST_0)
 * d[5] (CONST_1)
 * d[6] (CNT)
 *
 * Tape writes (all via %hhn):
 * RUN <- from `d[RUN] = 0; [byte %2$<-d[4], ptr %3$]` via `%1$*2$s%3$hhn` (ptr arg %3$)
 * CNT <- from `pop_print("A\n"); [counter-sync]` via `%5$hhn` (ptr arg %5$)
 *
 * Data-dependent formatting-time selection (%.*s gates):
 * (none)
 *
 * Unconditional formatting prints (%s):
 * from `pop_print("A\n");` via `%4$s` (str arg %4$)
 *
 * Raw tape-in (Gate E): (none)
 *
 * Cheat audit:
 * - Per-iteration helper calls: no
 * - table[d[i]] argument selections: no
 * - C-side semantic operators (comparison/bool): none
 * - C-side per-tick expression evaluation (state/branch): none
 * - C-side input semantics beyond raw tape-in: no
 * - Backend write sequencing: VM-pure counter reset (%r+u8(prev) trick)
 * - VM-pure mode: enabled
 * - VM-pure backend: micro
 * - Host-driven state snapshot input: no
 * - Tape byte changes outside %n writes: no
 */

static signed char d[7] = {1, 0, 0, 0, 0, 1, 0};
static const char z[] = "";
static const char r[] = "                                                                                                                                                                                                                                                                ";

static const char *fmt =
    "%1$*2$s%3$hhn"
    "%4$s"
    "%5$hhn"
;

int main(void) {
    while (*d)
        printf(fmt,
               z,
               ((unsigned)((unsigned char)((int)(unsigned char)d[4]))),
               d + 0,
               "A\n",
               d + 6);
    return 0;
}
