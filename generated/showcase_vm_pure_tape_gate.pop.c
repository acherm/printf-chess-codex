#include <stdio.h>

/*
 * Generated by tools/c2pop.py from: examples/showcase_vm_pure_tape_gate.c
 *
 * Source loop statements:
 *   pop_read_byte(IN_MSG);
 *   d[RUN] = 0;
 *   pop_print_if(d[IN_MSG], "T");
 *
 * POP Compliance Report
 * Canonical loop: while (*d) { d[1] = (signed char)getchar(); printf(fmt, ARGS); }
 * Purity score: 10/10 (POP-pure)
 * - Loop purity: 2/2
 * - Mutation purity: 2/2
 * - Control/branch purity: 2/2
 * - Engine purity: 2/2
 * - No C-script ROM: 2/2
 * Tape bytes:
 * d[0] (RUN)
 * d[1] (IN_MSG)
 * d[2] (IDX_2)
 * d[3] (IDX_3)
 * d[4] (IDX_4)
 * d[5] (IDX_5)
 * d[6] (IDX_6)
 * d[7] (IDX_7)
 *
 * Tape writes (all via %hhn):
 * RUN <- from `d[RUN] = 0;` via `%1$*2$s%3$hhn` (pad arg %2$, ptr arg %3$)
 *
 * Data-dependent formatting-time selection (%.*s gates):
 * from `pop_print_if(d[IN_MSG], "T");` via `%5$.*4$s` (prec arg %4$, str arg %5$)
 *
 * Unconditional formatting prints (%s):
 * (none)
 *
 * Raw tape-in (Gate E):
 * d[1] (IN_MSG) <- getchar() byte before each printf tick
 * EOF is preserved as raw byte value (signed char)EOF.
 *
 * Cheat audit:
 * - Per-iteration helper calls: no
 * - table[d[i]] argument selections: no
 * - C-side semantic operators (comparison/bool): none
 * - C-side per-tick expression evaluation (state/branch): none
 * - C-side input semantics beyond raw tape-in: no
 * - Backend write sequencing: single-write/no sequencing needed
 * - VM-pure mode: enabled
 * - Host-driven state snapshot input: no
 * - Tape byte changes outside %n writes: d[1] raw input bytes only (Gate E exception)
 */

static signed char d[8] = {1, 0, 0, 0, 0, 0, 0, 0};
static const char z[] = "";


static const char *fmt =
    "%1$*2$s%3$hhn"
    "%5$.*4$s"
;

int main(void) {
    int in_ch = 0;
    while (*d) {
        in_ch = getchar();
        d[1] = (signed char)in_ch;
        printf(fmt,
               z,
               ((unsigned)((unsigned char)(0))),
               d + 0,
               ((unsigned)((unsigned char)((int)(unsigned char)d[1]))),
               "T");
    }
    return 0;
}
